<div class="container">
  <h1>Cinematic</h1>
  <% @lists.each do |list| %>
    <p><%= link_to list.name, list_path(list) %></p>
  <% end %>
  <%= link_to "Add a New List", new_list_path, class: "btn btn-primary" %>
</div>
